import PicGo from '../core/PicGo';
import { Result } from '../utils/interfaces';
declare class PluginHandler {
    ctx: PicGo;
    constructor(ctx: PicGo);
    install(plugins: string[], proxy: string): Promise<void>;
    uninstall(plugins: string[]): Promise<void>;
    update(plugins: string[]): Promise<void>;
    execCommand(cmd: string, modules: string[], where: string, proxy?: string): Promise<Result>;
}
export default PluginHandler;
