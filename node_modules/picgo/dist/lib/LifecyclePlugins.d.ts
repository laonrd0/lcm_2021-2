import { Plugin } from '../utils/interfaces';
declare class LifecyclePlugins {
    list: {
        [propName: string]: Plugin;
    };
    name: string;
    constructor(name: string);
    register(id: string, plugin: Plugin): void;
    get(id: string): Plugin;
    getList(): Plugin[];
    getIdList(): string[];
}
export default LifecyclePlugins;
